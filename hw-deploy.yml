apiVersion: apps/v1
kind: Deployment
metadata:
  name: hw-deploy
  labels:
    app: homework
spec:
  replicas: 2
  selector:
    matchLabels:
      app: homework
  template:
    metadata:
      labels:
        app: homework
    spec:
      containers:
      - name: homework
        image: "homeimage:1.0.0"
        ports:
        - containerPort: 80
